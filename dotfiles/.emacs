; tell Emacs to consider the alt/option key as the M- modifier key
(setq mac-option-modifier 'meta)
; tell Emacs not to intercept super/command key presses (which normally it treats as the M- modifier key)
(setq mac-command-modifier nil)
